name: rentabilidad_flete
description: MVP mobile app to quote freight trips and decide profitability.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_maps_flutter: ^2.10.0
  http: ^1.2.2
  intl: ^0.20.1
  supabase_flutter: 2.15.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
